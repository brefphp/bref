---
title: PHP runtimes for Lambda
description: Run serverless PHP functions on AWS Lambda using Bref.
---

import { Callout, Tabs } from 'nextra/components'

# PHP runtimes for Lambda

This page exercises the v2 -> v4 component rename: `<Tab>` -> `<Tabs.Tab>`.

<Callout type="info">
    Callout import path is unchanged (`nextra/components`).
</Callout>

<Tabs items={['Composer', 'Manual']}>
    <Tabs.Tab>
        ```bash
        composer require bref/bref
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        Download the release manually.
    </Tabs.Tab>
</Tabs>
