---
title: What is Bref and serverless?
description: Bref helps you deploy PHP applications to AWS Lambda in a serverless way.
---

# What is Bref and serverless?

This is a plain docs page. It renders inside the docs `<Layout>` with sidebar, TOC,
breadcrumb and pagination — the default docs chrome.

Its `description` frontmatter flows through to a `<meta name="description">` tag, and the
title renders as `What is Bref and serverless? – Bref`.

## A heading

Some body text so the table of contents has something to show.
